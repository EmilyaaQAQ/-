修改/新建文件清单 (共 16 个文件)

  LIMIT 支持 (Phase 1)

  ┌─────┬───────────────────────────────────────────────────────┬──────┬─────────────────────────────────────────────┐
  │  #  │                         文件                          │ 操作 │                    说明                     │
  ├─────┼───────────────────────────────────────────────────────┼──────┼─────────────────────────────────────────────┤
  │ 1   │ src/observer/sql/parser/lex_sql.l                     │ 修改 │ 添加 LIMIT token                            │
  ├─────┼───────────────────────────────────────────────────────┼──────┼─────────────────────────────────────────────┤
  │ 2   │ src/observer/sql/parser/yacc_sql.y                    │ 修改 │ 添加 LIMIT token, limit_clause              │
  │     │                                                       │      │ 规则，select_stmt 集成                      │
  ├─────┼───────────────────────────────────────────────────────┼──────┼─────────────────────────────────────────────┤
  │ 3   │ src/observer/sql/parser/parse_defs.h                  │ 修改 │ SelectSqlNode 添加 limit 字段               │
  ├─────┼───────────────────────────────────────────────────────┼──────┼─────────────────────────────────────────────┤
  │ 4   │ src/observer/sql/stmt/select_stmt.h                   │ 修改 │ 添加 limit_ 成员和 limit()/set_limit() 方法 │
  ├─────┼───────────────────────────────────────────────────────┼──────┼─────────────────────────────────────────────┤
  │ 5   │ src/observer/sql/stmt/select_stmt.cpp                 │ 修改 │ 从 SelectSqlNode 传递 limit 到 SelectStmt   │
  ├─────┼───────────────────────────────────────────────────────┼──────┼─────────────────────────────────────────────┤
  │ 6   │ src/observer/sql/operator/logical_operator.h          │ 修改 │ 添加 limit_ 成员和 limit()/set_limit()      │
  ├─────┼───────────────────────────────────────────────────────┼──────┼─────────────────────────────────────────────┤
  │ 7   │ src/observer/sql/operator/physical_operator.h         │ 修改 │ PhysicalOperatorType 添加 LIMIT 和          │
  │     │                                                       │      │ VECTOR_INDEX_SCAN                           │
  ├─────┼───────────────────────────────────────────────────────┼──────┼─────────────────────────────────────────────┤
  │ 8   │ src/observer/sql/operator/limit_physical_operator.h   │ 新建 │ LimitPhysicalOperator 声明                  │
  ├─────┼───────────────────────────────────────────────────────┼──────┼─────────────────────────────────────────────┤
  │ 9   │ src/observer/sql/operator/limit_physical_operator.cpp │ 新建 │ LIMIT 截断实现（count_ 计数，达阈值后返回   │
  │     │                                                       │      │ RECORD_EOF）                                │
  ├─────┼───────────────────────────────────────────────────────┼──────┼─────────────────────────────────────────────┤
  │ 10  │ src/observer/sql/optimizer/logical_plan_generator.cpp │ 修改 │ 从 SelectStmt 传递 limit 到逻辑算子         │
  ├─────┼───────────────────────────────────────────────────────┼──────┼─────────────────────────────────────────────┤
  │ 11  │ src/observer/sql/optimizer/optimize_stage.cpp         │ 修改 │ 物理计划生成后包裹 LimitPhysicalOperator    │
  └─────┴───────────────────────────────────────────────────────┴──────┴─────────────────────────────────────────────┘

  索引基础设施 (Phase 2)

  ┌─────┬───────────────────────────────────────────┬──────┬─────────────────────────────────────────────────────────┐
  │  #  │                   文件                    │ 操作 │                          说明                           │
  ├─────┼───────────────────────────────────────────┼──────┼─────────────────────────────────────────────────────────┤
  │ 12  │ src/observer/storage/index/index_meta.h   │ 修改 │ 添加 IndexType enum、lists_/probes_/distance_type_ 字段 │
  ├─────┼───────────────────────────────────────────┼──────┼─────────────────────────────────────────────────────────┤
  │ 13  │ src/observer/storage/index/index_meta.cpp │ 修改 │ 更新 init()、to_json()、from_json()、desc()             │
  ├─────┼───────────────────────────────────────────┼──────┼─────────────────────────────────────────────────────────┤
  │ 14  │ src/observer/storage/index/index.h        │ 修改 │ 添加 virtual ann_search() 方法到 Index 基类             │
  └─────┴───────────────────────────────────────────┴──────┴─────────────────────────────────────────────────────────┘

  IVF-Flat 索引实现 (Phase 3)

  ┌─────┬──────────────────────────────────────────────────┬──────┬──────────────────────────────────────────────────┐
  │  #  │                       文件                       │ 操作 │                       说明                       │
  ├─────┼──────────────────────────────────────────────────┼──────┼──────────────────────────────────────────────────┤
  │ 15  │ src/observer/storage/index/ivfflat_index.h       │ 重写 │ IvfflatIndex 类声明（K-Means、ANN                │
  │     │                                                  │      │ search、倒排列表）                               │
  ├─────┼──────────────────────────────────────────────────┼──────┼──────────────────────────────────────────────────┤
  │ 16  │ src/observer/storage/index/ivfflat_index.cpp     │ 新建 │ 完整实现：K-Means 聚类训练、ANN 搜索、文件序列化 │
  ├─────┼──────────────────────────────────────────────────┼──────┼──────────────────────────────────────────────────┤
  │ 17  │ src/observer/storage/table/table.h               │ 修改 │ 添加 create_index(Trx*, IndexMeta&, FieldMeta*)  │
  │     │                                                  │      │ 重载                                             │
  ├─────┼──────────────────────────────────────────────────┼──────┼──────────────────────────────────────────────────┤
  │ 18  │ src/observer/storage/table/table.cpp             │ 修改 │ 新增重载方法，透传至 engine                      │
  ├─────┼──────────────────────────────────────────────────┼──────┼──────────────────────────────────────────────────┤
  │ 19  │ src/observer/storage/table/table_engine.h        │ 修改 │ 添加 virtual create_index(Trx*, IndexMeta&,      │
  │     │                                                  │      │ FieldMeta*)                                      │
  ├─────┼──────────────────────────────────────────────────┼──────┼──────────────────────────────────────────────────┤
  │ 20  │ src/observer/storage/table/heap_table_engine.h   │ 修改 │ 添加重载 create_index 声明                       │
  ├─────┼──────────────────────────────────────────────────┼──────┼──────────────────────────────────────────────────┤
  │ 21  │ src/observer/storage/table/heap_table_engine.cpp │ 修改 │ open() 分支 IndexType 创建索引；新增             │
  │     │                                                  │      │ create_index(IndexMeta&) 实现                    │
  └─────┴──────────────────────────────────────────────────┴──────┴──────────────────────────────────────────────────┘

  ---
  核心算法实现

  K-Means 聚类训练 (ivfflat_index.cpp::train_centroids)：
  - 随机初始化 k 个质心
  - 迭代：分配向量到最近质心 → 更新质心为簇内均值
  - 空簇重新初始化（从随机向量）
  - 最多 20 次迭代或收敛

  ANN 搜索 (ivfflat_index.cpp::ann_search)：
  - 计算查询向量到所有质心的距离
  - 选取 probes_ 个最近质心
  - 扫描对应倒排列表中的所有向量
  - 精确计算距离、排序、返回 Top-K (RID, distance)

  距离计算 (compute_distance)：
  - EUCLIDEAN: sqrt(Σ(aᵢ−bᵢ)²)
  - COSINE: 1 − (a·b)/(‖a‖×‖b‖)
  - DOT: −Σ(aᵢ×bᵢ) (负内积，用于排序)

  文件持久化 (save_to_file / load_from_file)：
  - 二进制格式：lists_ → probes_ → dim_ → centroids → inverted_lists
  - 支持服务器重启后重新加载索引
